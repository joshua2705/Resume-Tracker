fastapi>=0.111.0
uvicorn[standard]>=0.30.1
pydantic>=2.12.5
python-multipart==0.0.9
python-dotenv==1.0.1
google-genai>=1.0.0
llama-parse>=0.5.0

# --------------------------------------------------------------------------- #
# Agentic layer (LangGraph). All OPTIONAL: if these aren't installed or no keys
# are set, the app falls back to the existing heuristic/Gemini path and still
# runs fully offline. Install with: pip install -r requirements.txt
# --------------------------------------------------------------------------- #
langgraph>=0.2.50                 # graph orchestration + prebuilt ReAct agent
langchain>=0.3.0                  # core (tools, messages)
langchain-core>=0.3.0
langchain-google-genai>=2.0.0     # Gemini chat model for the agents
langchain-mcp-adapters>=0.1.0     # wraps the Gmail MCP server as LangChain tools
langsmith>=0.1.100                # tracing/logging of every agent run
