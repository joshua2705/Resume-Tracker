fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
python-multipart==0.0.9
python-dotenv==1.0.1
# Real integrations. Recent versions matter — the new google-genai client API
# (genai.Client(...).models.generate_content) only exists in >=1.0.
google-genai>=1.0.0
llama-parse>=0.5.0
